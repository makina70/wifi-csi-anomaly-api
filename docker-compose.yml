services:
  ml-api:
    build: .
    ports:
      - "8001:8001"
    environment:
      MODEL_PATH: /app/models/feature_ae_w500/model.pt
      MODEL_DEVICE: cpu
      ADAPTIVE_FEATURE_CALIBRATION: "true"
      CALIBRATION_WINDOWS: "20"
      CALIBRATION_LOWER_QUANTILE: "0.20"
      CALIBRATION_LOWER_MULTIPLIER: "0.80"
      CALIBRATION_QUANTILE: "0.80"
      CALIBRATION_MULTIPLIER: "1.00"
    restart: unless-stopped
